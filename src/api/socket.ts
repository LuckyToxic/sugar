// import { io } from "socket.io-client";

// const baseUrl = import.meta.env.VITE_BASE_URL;

// const token = localStorage.getItem("token");
// const sessionId = localStorage.getItem("sessionId"); // или где ты хранишь токен

// export const socket = io(`${baseUrl}/gpt/connect?session_id=${sessionId}`, {
//   extraHeaders: {
//     Authorization: `${token}`,
//   },
// });
